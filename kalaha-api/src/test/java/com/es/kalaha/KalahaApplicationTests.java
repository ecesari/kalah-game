package com.es.kalaha;

import org.springframework.boot.test.context.SpringBootTest;

import static org.assertj.core.api.Assertions.assertThat;
import static org.junit.jupiter.api.Assertions.assertNotNull;

@SpringBootTest
class KalahaApplicationTests {

	/*private House house;
	@Test
	void contextLoads() {

	}

	@Test
	void GivenSeedNumber_ShouldReturnHouse(){
		var house = new House(6);
		assertThat(house.getSeeds()).isEqualTo(6);
	}

	@Test
	void GivenStoreCreate_ShouldReturnZeroSeeds(){
		var store = new Store();
		assertThat(store.getSeeds()).isEqualTo(0);
		var store2 = store.getNext();
	}
*/


}
